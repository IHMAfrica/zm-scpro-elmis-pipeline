apiVersion: v1
kind: Service
metadata:
  name: {{ include "zm-scpro-elmis-pipeline-chart.fullname" . }}
  labels:
    {{- include "zm-scpro-elmis-pipeline-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zm-scpro-elmis-pipeline-chart.selectorLabels" . | nindent 4 }}
